import React from 'react';
import ReactDOM from 'react-dom';

// Compoents
import Galery from './components/Galery';

const root = document.getElementById('root')
const element = <Galery />

ReactDOM.render(element, root);
